---
title: "Get Current User"
description: "Retrieve the authenticated user's information"
---

## GET /api/auth/me

Returns the profile information for the currently authenticated user.

<Warning>
  This endpoint requires authentication. Include a valid access token in the Authorization header.
</Warning>

### Authentication

This endpoint requires a valid JWT access token obtained from the [login endpoint](/api-reference/auth/login).

Include the token in the request header:
```
Authorization: Bearer <access_token>
```

### Response

<ResponseField name="user_id" type="long">
  Unique identifier for the user
</ResponseField>

<ResponseField name="username" type="string">
  Username of the user
</ResponseField>

<ResponseField name="email" type="string">
  Email address of the user
</ResponseField>

<ResponseField name="full_name" type="string" nullable>
  Full name of the user
</ResponseField>

<ResponseField name="avatar_url" type="string" nullable>
  URL to the user's avatar image
</ResponseField>

<ResponseField name="is_active" type="boolean">
  Whether the user account is active
</ResponseField>

<ResponseField name="email_verified" type="boolean">
  Whether the user's email has been verified
</ResponseField>

<ResponseField name="created_at" type="datetime">
  Timestamp when the user account was created
</ResponseField>

### Request Example

<CodeGroup>
```bash cURL
curl -X GET https://api.taskpro.com/api/auth/me \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

```javascript JavaScript
const token = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';

const response = await fetch('https://api.taskpro.com/api/auth/me', {
  method: 'GET',
  headers: {
    'Authorization': `Bearer ${token}`
  }
});

const userData = await response.json();
```

```python Python
import requests

token = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'

response = requests.get(
    'https://api.taskpro.com/api/auth/me',
    headers={'Authorization': f'Bearer {token}'}
)

user_data = response.json()
```
</CodeGroup>

### Response Example

<CodeGroup>
```json 200 - Success
{
  "user_id": 123,
  "username": "johndoe",
  "email": "john.doe@example.com",
  "full_name": "John Doe",
  "avatar_url": "https://cdn.taskpro.com/avatars/johndoe.png",
  "is_active": true,
  "email_verified": true,
  "created_at": "2026-01-15T08:30:00"
}
```

```json 401 - Unauthorized (Missing Token)
{
  "detail": "Could not validate credentials"
}
```

```json 401 - Unauthorized (Invalid Token)
{
  "detail": "Could not validate credentials"
}
```
</CodeGroup>

### Status Codes

| Status Code | Description |
|-------------|-------------|
| 200 | User information retrieved successfully |
| 401 | Missing, invalid, or expired access token |
| 500 | Internal server error |

<Info>
  This endpoint is useful for:
  - Verifying token validity
  - Retrieving updated user profile information
  - Checking user authentication status
</Info>